---
name: portfolio-state
description: State + design system of the ERP-consultant portfolio (project1–5.html), as of June 2025
metadata:
  type: project
---

Cee's portfolio = `index.html` + `project1–5.html` + `senior-project.html`, GitHub Pages site (deploys from `main`). Design identity ("Engineer's Console"): **Implementation Blue `#2563eb`** (dark `#60a5fa`) on slate, **Inter + JetBrains Mono**, flat-by-default with accent glow. See DESIGN.md / [[impeccable-design-skill]].

Per-page scoped `<style>` blocks layer the upgrades (don't touch shared styles.css unless intended): `pN-lux` (luxury: KPI before→after delta cells, hero spec-card + status pulse, callout chips, code→editor-window `lx-code`, slop fixes), `pN-exh` (NDA exhibits), plus page-specific (`p1-pr` order entry, `p1-mrp`, `p1-ps` procurement sim, `p2-arch` system-architecture). Reusable class prefix is `lx-`.

**Done (luxury pass + exhibits, detector-clean):** P1 (Order Entry, Reorder/EOQ worked-calc, Vendor Scorecard, MRP, Procurement Simulation), P2 (Pricing Conditions, AR Aging, 480→15 basis, **System Architecture A–E**, model = Base 350k + Z001 sibling −50k / Z002 group −75k on the 3rd / Z003 referral −15–30k, best single discount), P3 (Defect Log + KPI reframe), P4 (P&L + Cost Allocation, collection 82.4% marked ongoing).

**Remaining: P5** (FPDU award project — different template, real photos exist). Needs: SQCDE matrix + FMEA worksheet (recreation), award-as-text, and `[CONFIRM]` on SQCDE sub-scores / FMEA S·O·D / ฿1M risk-cost derivation.
