---
target: index.html
total_score: 31
p0_count: 0
p1_count: 0
timestamp: 2026-06-01T23-10-14Z
slug: index-html
---
## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | States + theme toggle; scrollspy active states; mostly static |
| 2 | Match System / Real World | 3 | Domain-correct; jargon-dense for a non-ERP reader |
| 3 | User Control and Freedom | 3 | Free anchor nav; mobile drawer opens/closes |
| 4 | Consistency and Standards | 4 | Coherent tokens, fonts render, de-scaffolded; nav now works on mobile |
| 5 | Error Prevention | 3 | n/a — mailto/tel only; img onerror fallback |
| 6 | Recognition Rather Than Recall | 3 | Clear labelled nav; Projects-first ordering |
| 7 | Flexibility and Efficiency | 3 | Persistent Contact CTA + working mobile nav. Still no résumé/CV download |
| 8 | Aesthetic and Minimalist Design | 3 | Real type system, no scaffolding, differentiated layouts; long scroll + intentional repetition cap it |
| 9 | Error Recovery | 3 | n/a; graceful image fallback |
| 10 | Help and Documentation | 3 | n/a for a portfolio |
| **Total** | | **31/40** | **Good — conversion (CV) and scroll length are the remaining cap** |

## Anti-Patterns Verdict

**Does this look AI-generated? No.** The verdict held from the previous pass: real type system rendering, no numbered-eyebrow scaffolding, differentiated section layouts. Nothing in the nav/mobile work reintroduced a tell. The drawer's near-opaque backdrop is functional (legibility over content), not decorative glassmorphism.

**Deterministic scan (detect.mjs, exit 0):**
- `em-dash-overuse`: clear.
- `numbered-section-markers` (advisory): only the per-card ordinals on Projects (01–04) and Achievements (01–05) — kept intentionally.
- `overused-font` (warning): Inter — committed identity, acknowledged.

**Visual overlays:** Not available (no browser automation); findings from source review + scan.

## Overall Impression

Solid and credible. This pass closed two of the prior gaps: the recruiter now has a Projects-first nav and an always-visible Contact CTA, and the mobile nav no longer overflows (CSS-only hamburger drawer). The single remaining high-value gap is unchanged: a recruiter still cannot download a CV, and the proof lives inside a long scroll.

## What's Working

1. **Recruiter-first navigation.** Projects leads; Contact is a persistent accent CTA in the fixed nav — a fast, always-reachable conversion path.
2. **Mobile nav fixed.** The 6-link bar collapses into a keyboard-operable hamburger drawer that auto-closes on tap; desktop untouched. Resolves the Casey (mobile) red flag.
3. **Distinctive, de-scaffolded content** (carried from prior passes): doc hierarchy, defect kanban, differentiated Methodology/Focus layouts, real fonts.

## Priority Issues

- **[P2] No résumé / CV download.** Still the top gap. The Contact CTA jumps to email/phone, but recruiters expect a downloadable CV. **Fix:** add a "Download CV (PDF)" button in the hero CTA row and/or contact section (drop a real PDF into the project). **Suggested command:** /impeccable craft (CV-download affordance).

- **[P2] Scroll length to proof.** 11 sections remain; metric repetition is intentional (kept), but the path from top to a decision is long. **Fix:** optional — consider whether two adjacent sections (e.g. Impact + Achievements) could merge without losing the intentional repetition. **Suggested command:** /impeccable distill (light touch) — or leave as a deliberate choice.

- **[P3] Reduced-motion coverage.** Confirm the animated `#techCanvas` background and the new drawer slide honor `prefers-reduced-motion`. **Suggested command:** /impeccable audit.

## Persona Red Flags

**Khun Ploy (Recruiter):** Path improved (Projects-first, Contact CTA). Still cannot grab a CV — the one missing expected action.
**Casey (Mobile):** Nav overflow resolved via the drawer. Worth a real-device confirmation of the open/close + tap-to-navigate.
**Sam (Accessibility):** Captions pass AA; nav drawer is keyboard-focusable with a focus ring. Confirm `prefers-reduced-motion` for canvas + drawer.
**Jordan (non-ERP reader):** Jargon density unchanged (acceptable; plain-language proof present).

## Minor Observations

- Scattered inline `style="..."` blocks remain (minor consistency cost).
- Confirm public contact email (`chatree.kengpipat@gmail.com`) is intended.

## Questions to Consider

- The conversion path now ends at email/phone. Would a "Download CV (PDF)" button, visible in the hero, convert more recruiters than a scroll-to-contact?
- With Projects leading the nav, does the page-body order (Impact → About → Method → Projects) still match? Should Projects move higher in the body, not just the nav?
